package model.units;

import model.items.IEquipableItem;
import model.map.Location;

public abstract class HitPoints{
  /**
   * Create Struct of the healph
   *
   * @param hitPoints the maximum amount of damage a unit can sustain
   * @param
   */
}
